using System;

namespace FizzBuzz
{
    /// <summary>
    /// FizzBuzzer is responsible for generating strings!
    /// If the number passed in is divisible by 3 return Fizz
    /// If the number passed in is divisible by 5 return Buzz
    /// If the number passed in is divisible by both 3 and 5 then return Fizz Buzz
    /// If none of the above are true, return an empty string.
    /// </summary>
    public class FizzBuzzer
    {
        public string Calculate(int number)
        {
            throw new NotImplementedException();
        }
    }
}
